<!DOCTYPE html>
<html lang="en">

<head>
    <% include partials/head %>
</head>

<body>
    <header>
        <% include partials/header %>
    </header>
    <main>
        <div class="row" style="margin: 7rem">
            <% recreationCollection.forEach(function(recreation) { %>
            <div class="col-md-3 mt-5">
                <div class="card-deck">
                    <div class="card">
                        <a data-toggle="modal" data-target="#exampleModal" style="cursor: pointer">
                            <img src="<%= recreation.image %>" class="card-img-top" alt="<%= recreation.name %>">
                        </a>
                        <div class="card-body">
                            <h5 class="card-title text-center">
                                <a href="/recreation/<%= recreation._id %>" class="text-muted text-decoration-none">
                                    <%= recreation.name %></a>
                            </h5>
                            <p class="card-text">
                                <%= recreation.country %>
                            </p>
                            <p class="card-text">
                                <%= recreation.city %>
                            </p>
                        </div>
                        <!-- Modal -->
                        <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog"
                            aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="exampleModalLabel">
                                            <%= recreation.name %>
                                        </h5>
                                    </div>
                                    <div class="modal-body">
                                        <div class="modal-body">
                                            <img src="<%= recreation.image %>" class="card-img-top"
                                                alt="<%= recreation.name %>">
                                            <p>
                                                <%= recreation.description %>
                                            </p>
                                        </div>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary"
                                            data-dismiss="modal">Close</button>
                                        <button type="button" class="btn btn-primary">Save changes</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </main>
    <footer>
        <% include partials/footer %>
    </footer>
</body>

</html>